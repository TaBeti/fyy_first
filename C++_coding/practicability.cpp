#include <iostream>
using namespace std;

//C语⾔中的变量都必须在作⽤域开始的位置定义！！
//C++中更强调语⾔的“实⽤性”，所有的变量都可以在需要使⽤时再定义。

int main(){
    int i = 0;
    cout << "i = " << i << endl;

    int k;
    k = 4;
    cout << "k = " << k << endl;

    return 0;
}